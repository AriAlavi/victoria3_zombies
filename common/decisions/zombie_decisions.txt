peace_zombies_decision = {
	is_shown = {
		exists = c:ZOM
		has_war_with = c:ZOM
		is_subject = no
	}

	possible = {
		OR = {
			AND = {
				any_subject_or_below = {
					any_scope_war = {
						OR = {
							NOT = { attacker_warleader = {
								country_has_primary_culture = zombie
							}}
							any_scope_front = {
								always = no
							}
						}
					}
				}

			}
		}

	}

	when_taken = {
		trigger_event = { id = zombie.6 days = 0 popup = no }
	}

	ai_chance = {
		base = 100		
	}
}
